import "./src/lib.js"

print(it.mul$a.it([3, 4]))
print(it.mul$a()([3, 4]))

// "foo bar baz".split(" ").map(get.chars).pr
// print([1, 2, 3].map(it.mul$d(it)))
// print([1, 2, 3].map(it.mul$fd(it.add(1))))
// print("hello test foo".split(" ").map(length.it))
// print(["hello world", "foo bar baz"].map(split(" ").map(esplit(""))))
